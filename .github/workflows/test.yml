name: CI

on:
  push:
    branches:
      - master
  pull_request:
  workflow_dispatch:

jobs:
  test:
    name: Tests on ${{ matrix.go-version }} ${{ matrix.platform }}
    strategy:
      matrix:
        go-version: ['1.25']
        platform: [ubuntu-latest, macos-latest, windows-2022]
        # TODO: add windows-11-arm after fixing clang -mthreads issue
    runs-on: ${{ matrix.platform }}

    steps:
      - name: Install Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go-version }}

      - name: Checkout
        uses: actions/checkout@v4

      - name: Unzip libv8
        shell: bash
        run: |
          files=$(find . -name "libv8*.zip")
          for file in $files; do
            dir=$(dirname "$file")
            echo "Extracting $file to directory $dir"
            unzip -o -d "$dir" "$file"
          done

      - name: Go Test (Unix)
        if: runner.os != 'Windows'
        env:
          CGO_CXXFLAGS: "-Werror"
        run: go test -v -coverprofile c.out ./...

      - name: Go Test (Windows)
        if: runner.os == 'Windows'
        env:
          CGO_ENABLED: '1'
          CXX: clang++ --target=x86_64-pc-windows-msvc -fno-exceptions
        run: go test -v -ldflags "-extld g++" ./...

      - name: Upload coverage to Codecov
        if: runner.os != 'Windows'
        uses: codecov/codecov-action@v1
        env:
          OS: ${{ matrix.platform }}
          GO: ${{ matrix.go-version }}
        with:
          files: ./c.out
          env_vars: OS,GO

      - name: Add GOPATH to GITHUB_ENV
        if: runner.os != 'Windows'
        run: echo "GOPATH=$(go env GOPATH)" >>"$GITHUB_ENV"

      - name: Scan and upload FOSSA data (Linux/Mac)
        if: runner.os != 'Windows' && env.FOSSA_API_KEY != '' && github.ref == 'refs/heads/master'
        run: |
          curl -H 'Cache-Control: no-cache' https://raw.githubusercontent.com/fossas/fossa-cli/master/install.sh | sudo bash
          fossa analyze
        env:
          FOSSA_API_KEY: ${{ secrets.FOSSA_API_KEY }}
